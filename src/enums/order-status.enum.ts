export enum OrderStatus {
  draft = 'DRAFT',
  confirmed = 'CONFIRMED',
  accepted = 'ACCEPTED',
  ready = 'READY_FOR_DISPATCH',
  dispatched = 'DISPATCHED',
  fulfilled = 'FULFILLED',
  cancelled = 'CANCELLED',
}
