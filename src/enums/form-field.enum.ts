export enum FormFieldType {
  heading = 'heading',
  string = 'string',
  richText = 'rich-text',
  number = 'number',
  boolean = 'boolean',
  date = 'date',
  array = 'array',
  object = 'object',
  image = 'image',
  modifier = 'modifier',
  radio = 'radio',
  select = 'select',
}
